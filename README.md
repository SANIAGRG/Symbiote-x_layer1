Layer 1 of a 4-layer intelligence perception platform, building a multi-modal scenario detection model that analyzes text and images to assess threat levels with confidence scoring
Implemented object detection for persons, vehicles, and aircraft alongside a text-based threat classifier, generating structured JSON reports consumed by downstream layers.
